[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

# @netlify/database auto-provisions free Postgres on deploy.
# Optional env vars in the Netlify UI:
#   NEXT_PUBLIC_APP_URL
#   SPOTIFY_CLIENT_ID
#   SPOTIFY_CLIENT_SECRET
